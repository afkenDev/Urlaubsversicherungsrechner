 Seite 1: 
 
    <div className="App">
      <div className="App-header">
        <h1 className="header logo">Urlaubsversicherungsrechner</h1>
        <div className="steps"></div>
        <div className="frage">
          Wollen Sie sich im Ausland sicher fühlen?
        </div>
        <Button onClick={handleButtonClick}>Start ⭢</Button>
        {animatePlane && <PlaneImage onAnimationEnd={handleAnimationEnd} />}
      </div>
    </div>


Seite 2:


  <div className="App">
    <div className="App-header">
      <h1 className="header logo">Wie viele Personen wollen Sie mitversichern?</h1>
      <div className="steps"></div>
      <div className="frage">
        Beachten Sie bitte, dass Sie nur Leute von Ihrem Haushalt hinzufügen dürfen. Haustiere sind u. a. Katzen und Hunde. 
      </div>
      <div className="form-container">
      <div className="form-group">
        <label htmlFor="erwachsene">Erwachsene (17+)</label>
        <input type="number" id="erwachsene" min="0" defaultValue="0" />
        <button className="btn" onClick={() => increment('erwachsene')}>+</button>
        <button className="btn" onClick={() => decrement('erwachsene')}>-</button>
      </div>
      <div className="form-group">
        <label htmlFor="kinder">Kinder (17-)</label>
        <input type="number" id="kinder" min="0" defaultValue="0" />
        <button className="btn" onClick={() => increment('kinder')}>+</button>
        <button className="btn" onClick={() => decrement('kinder')}>-</button>
      </div>
      <div className="form-group">
        <label htmlFor="haustiere">Haustiere</label>
        <input type="number" id="haustiere" min="0" defaultValue="0" />
        <button className="btn" onClick={() => increment('haustiere')}>+</button>
        <button className="btn" onClick={() => decrement('haustiere')}>-</button>
      </div>
    </div>
      <Button onClick={handleButtonClick}>Weiter ⭢</Button>
    </div>
    
  </div>